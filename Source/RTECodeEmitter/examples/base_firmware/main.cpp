#include "state.h"
#include "platform_api.h"

#include <iostream>

AppState appState;

int main() {
    // RTE_EMIT: app_loop init

    while (true) {
        // RTE_EMIT: app_loop step
    }

    return 0;
}
