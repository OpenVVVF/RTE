{
  "id": "hw.encoder.decode",
  "displayName": "Encoder Decoder",
  "inlineCode": "const float counts_per_rad = counts_per_rev / 6.28318530718f;\nconst float dt_s = sample_time_s;\n\ntheta = static_cast<float>(raw_counts) / counts_per_rad;\n\nconst float delta = static_cast<float>(raw_counts) - prev_counts;\nomega = delta / (counts_per_rad * dt_s);\nprev_counts = static_cast<float>(raw_counts);\n",
  "constructorCode": "",
  "classHeader": "",
  "classDefinition": "",
  "maxInstances": 1,
  "isEntryPoint": false,
  "inputPorts": [
    {
      "name": "raw_counts",
      "direction": "input",
      "type": {"quantity": "dimensionless", "frame": "scalar", "dtype": "f32"}
    }
  ],
  "outputPorts": [
    {
      "name": "theta",
      "direction": "output",
      "type": {"quantity": "dimensionless", "frame": "scalar", "dtype": "f32"}
    },
    {
      "name": "omega",
      "direction": "output",
      "type": {"quantity": "angular_velocity", "frame": "scalar", "dtype": "f32"}
    }
  ]
}
